import React from 'react';

export default function Steps(props) {
  const { steps } = props;

  const formatSteps = (string) => {
    // steps
    console.log(steps)
  }
  formatSteps(steps)
  return(
    <div>
      <h2>Directions</h2>
      
    </div>
  );
}